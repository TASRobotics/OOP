public class Tree extends Static {
  Tree(PVector pos, int w, int h) {
    super(pos, w, h);
  }

  // FIXME
  public void display() {
    // cachedRender.beginDraw();
    // cachedRender.noStroke();
    // cachedRender.background(255, 0, 0);
  
    // // Base position: bottom center of the PGraphics
    // float centerX = cachedRender.width / 2.0;
    // float bottomY = cachedRender.height;
  
    // // Draw trunk
    // int trunkWidth = w-145;
    // int trunkHeight = h*3/4;

    // cachedRender.fill(139, 69, 19); // brown
    // cachedRender.rect(centerX-w/2.0, bottomY-h, trunkWidth, trunkHeight);
  
    // // Bush position (top of trunk)
    // float bushY = bottomY - h;
  
    // // Draw bushy top (green foliage)
    // cachedRender.fill(34, 139, 34); // forest green
    // cachedRender.ellipse(centerX, bushY - 30, 200, 150);
    // cachedRender.ellipse(centerX - 70, bushY - 10, 150, 120);
    // cachedRender.ellipse(centerX + 70, bushY - 10, 150, 120);
    
    // cachedRender.endDraw();
  }
}
